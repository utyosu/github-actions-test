name: CI

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: utyosu/build-rails-test:latest

    steps:
    - uses: actions/checkout@master
    - name: run test
      run: |
        echo $PATH
        . tools/cloud_build/test.sh
